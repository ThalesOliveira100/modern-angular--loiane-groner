Angular is now **signals-first**

	* Signals are no longer an experiment or an optional feature.
	* They are the default way Angular expects you to manage UI state.

like:

const count = signal(0);
const doubled = computed(() => count() * 2);